{
  "name": "pouchdb-sync-to-anything-browser-example",
  "version": "1.0.0",
  "description": "Browser example for pouchdb-sync-to-anything",
  "private": true,
  "main": "app.js",
  "scripts": {
    "build": "mkdir -p dist && browserify app.js > dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Karl Westin",
  "license": "MIT",
  "dependencies": {
    "browserify": "^14.1.0",
    "pouchdb": "^6.1.2"
  }
}
